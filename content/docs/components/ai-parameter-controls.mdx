---
title: AI Parameter Controls
description: A clean, elegant UI for adjusting common AI model parameters with visual controls and preset configurations.
---

import {
  BasicDemo,
  HorizontalDemo,
  CustomParametersDemo,
  CustomPresetsDemo,
} from "@/components/demos/ai-parameter-controls-demos";
import { TypeTable } from "fumadocs-ui/components/type-table";

## Installation

```bash
npx shadcn@latest add "https://haberui.com/h/ai-parameter-controls.json"
```

## Overview

AI Parameter Controls provides a clean, intuitive interface for users to adjust AI model parameters like temperature, top_p, and max_tokens. It includes:

- Visual sliders and inputs for each parameter
- Helpful tooltips explaining parameter effects
- Preset configurations for quick parameter sets
- Responsive vertical and horizontal layouts
- Reset controls to default values

## Examples

### Basic Usage

<div className="not-prose my-6">
  <BasicDemo />
</div>

### Horizontal Layout

<div className="not-prose my-6">
  <HorizontalDemo />
</div>

### Custom Parameters

<div className="not-prose my-6">
  <CustomParametersDemo />
</div>

### Custom Presets

<div className="not-prose my-6">
  <CustomPresetsDemo />
</div>

## Props

<TypeTable
  type={{
    parameters: {
      description: "Parameters to display (temperature, top_p, etc)",
      type: "AIParameter[]",
      required: true,
    },
    onParametersChange: {
      description: "Called when parameters are changed",
      type: "(parameters: AIParameter[]) => void",
    },
    presets: {
      description: "Available presets",
      type: "Preset[]",
      default: "DEFAULT_PRESETS",
    },
    onPresetChange: {
      description: "Called when a preset is selected",
      type: "(preset: Preset) => void",
    },
    showReset: {
      description: "Show the reset button",
      type: "boolean",
      default: "true",
    },
    showPresets: {
      description: "Show the presets selector",
      type: "boolean",
      default: "true",
    },
    resetLabel: {
      description: "Label for the reset button",
      type: "string",
      default: '"Reset to defaults"',
    },
    parameterClassName: {
      description: "Custom class for the parameter container",
      type: "string",
    },
    layout: {
      description: "Layout direction",
      type: '"horizontal" | "vertical"',
      default: '"vertical"',
    },
    decimalPlaces: {
      description: "Number of decimal places to display",
      type: "number",
      default: "1",
    },
  }}
/>

## Parameter Type

<TypeTable
  type={{
    name: {
      description: "Parameter name (temperature, top_p, etc.)",
      type: "string",
      required: true,
    },
    value: {
      description: "Current value",
      type: "number",
      required: true,
    },
    min: {
      description: "Minimum allowed value",
      type: "number",
      required: true,
    },
    max: {
      description: "Maximum allowed value",
      type: "number",
      required: true,
    },
    step: {
      description: "Step increment for sliders",
      type: "number",
      required: true,
    },
    description: {
      description: "Helpful description for tooltips",
      type: "string",
      required: true,
    },
  }}
/>

## Preset Type

<TypeTable
  type={{
    name: {
      description: 'Preset name (e.g., "Creative", "Precise")',
      type: "string",
      required: true,
    },
    description: {
      description: "Short description of the preset",
      type: "string",
      required: true,
    },
    parameters: {
      description: "Parameter values for this preset",
      type: "{ [key: string]: number }",
      required: true,
    },
  }}
/>
